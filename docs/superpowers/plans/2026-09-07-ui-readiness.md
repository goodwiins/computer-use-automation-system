# Capability availability and readable run results

Goal: implement approved UX unit 2 after baseline publication. Use the existing Next/Express/assistant-ui flow; no runtime execution, live target actions, backend auth rewrite or new dependencies.

Source: `/Users/goodwiinz/.codex/worktrees/interface-ai-next-chat-demo/docs/superpowers/plans/2026-09-07-meridian-ux-delivery.md`, Task 2 and global constraints. Worktree: `/Users/goodwiinz/.codex/worktrees/interface-ai-ui-readiness`, branch `codex/meridian-ui-readiness`. Implementation base is local integration `bca6d36a955727464dad533915413c2a32a234fc`, combining reviewed PR95 `0914e1d` and PR93 `dcb048c`. B1 remains a distinct backend dependency. The only merge conflict retained both adjacent login and member-identity test blocks; combined validation is still required.

### Task 1: Add truthful capability metadata and readable run presentation

Ownership: service.ts and the additive `/capabilities` response in http.ts; UI session.tsx/main.tsx/dashboard.tsx; a small shared capability-label module and UI presentation.ts; focused tests plus existing fake-service fixtures required by the new response. Do not edit chat routing, auth, approval dispatch, runtime or artifacts.

- [x] Reuse the existing seven public MERIDIAN capability IDs and move their label list to a small dependency-free shared module, so server availability and UI headings use one definition. No generic registration layer.
- [x] Keep catalog/tool schemas unchanged. Add `availability(principal)` returning the fixed public IDs with label/state/safe reason. Other app profiles return no MERIDIAN entries. Caller allowlist check MUST precede artifact lookup: restricted even if absent. An authorized absent artifact is `not_recorded` with “No approved recording”. Existing approved artifact is `available`, unless shutdown, one active operation, or capability-wide unknown quarantine makes it temporarily unavailable. Reasons contain no owner, member or run identity. A foreign unknown may block a capability but must not disclose which owner/run.
- [x] Include availability in authenticated GET `/capabilities`. Refresh catalog plus availability with authenticated run refresh and reconnect; use “Availability unavailable” on missing metadata, never infer unrecorded from absence. Disable new direct invocations when known unavailable or metadata refresh fails; unchanged server validation remains authoritative. Keep same-key recovery accessible and never turn an unknown outcome into retry permission.
- [x] Expose optional ISO `finishedAt` derived only from `live.finished` in shared get projection. Historical completion time is unavailable. Preserve PR93 private projection when integrated. If journal state is dispatching, do not label it completed or claim a result before verification; keep changes to runtime state semantics out of this presentation unit.
- [x] Add small `capabilityLabel`, `runPresentation` and `formatMoney` functions. Reuse exact decimal strings; group digits directly or use BigInt for whole units. Do not pass money through Number, parseFloat or cents conversion. Invalid strings remain unchanged. Test very large, negative, zero and malformed values. Label known fields (Share, Share type, Balance, Status, Member, Amount, Source share, Destination share…) without altering stored values.
- [x] Use readable headings/states and business outcome explanations. Preserve raw capability/state/code/run/version/step and exact output values in expandable Details. Generic failure/interrupted cannot claim no posting; unknown says “Unable to verify outcome” with separate read-only investigation and no retry. Only successful live read results show “Read completed at” from finishedAt. Retain withheld-history explanations and exact-member identity gating; do not add any inference or identity lookup here.
- [x] Cover access-before-existence, hidden artifact exclusion, foreign quarantine privacy, current availability refresh, large decimal formatting, failure/unknown language, live completion versus historical absence, escaped labels/values and unchanged invocation count through real service/focused browser tests. Update old presentation expectations only where intended. Existing approval/idempotency tests remain.
- [x] Run focused tests while iterating. Coordinate the heavy full-suite slot; run repository CI once after changes plus diff check. Write scoped commit/report and await independent review; coordinator owns publication and downstream integration.

Implementation boundaries: B1 already owns subject auth and will integrate `principalRole` for this method; do not demote ownership to a role string. The baseline's legacy contract stays intact in this unit. Missing live transfer/update/hold acceptance is external and cannot be claimed through these UI states. Live demo checkout/PID/evidence remain untouched.

### Task 2: Lookup-only recovery for an uncertain request

The final unit1 review exposed a plan defect: ordinary idempotent invoke can create a first operation when an earlier request never arrived. A recovery action advertised as status-only therefore needs a server lookup-only boundary.

Ownership: src/server/service.ts, the existing invoke HTTP schema/route in http.ts, dashboard.tsx recovery request/copy, and focused real-service/API/browser tests. Preserve auth, normalized identity, PR93 privacy, artifacts, journal layout and runtime behavior; no new endpoint/framework/dependency.

- [x] Add strict optional lookupOnly:true to the existing invoke JSON contract. Reuse invoke's existing principal/role/allowlist/argument normalization/request identity checks. Existing exact bound key returns the original run before availability/admission; absent key throws a safe404 before reservation, runtime, linked inquiry or any mutation. Wrong identity remains409; unauthorized callers remain403. Normal invocation and internal inquiry default to existing behavior.
- [x] UI recovery sends captured original capability/args/role/key plus lookupOnly:true. It never sends the mutable form or regenerates a key. Initial failure says acceptance is unconfirmed, not accepted; absent lookup says no accepted request was found rather than claiming completion/non-posting. New/changed actual submissions still require available authenticated metadata.
- [x] RED/GREEN tests cover lost-before-delivery and definite pre-reservation rejection, absent key produces no reserve/runtime/identity child, existing active/unknown returns same run, changed args/role/key owner cannot reuse another identity, malformed lookupOnly fails strict HTTP validation. Keep accepted-response-loss and changed-control browser regressions; inspect outgoing bodies and actual service journal count.
- [x] Run smallest covering tests plus both typechecks/build. Independent review and one combined final pre-push/hosted gate before publication. No live demo edits, real target requests or approval decisions.

Tasks 1 and 2 were published in PR97 at 724519663c2d2fd650923de0234a7ff5fad834fe. Independent final review, full pre-push CI (771 tests / 28 files, both typechecks, Next build), and exact-head hosted run 34087815152 passed. The review follow-up below remains outstanding; green CI does not resolve it.

### Task 3: Keep uncertain direct requests bound until explicit resolution

PR97 review3946890559 found that recoveryAvailable does not block changed normal submissions. A lost response may correspond to a completed operation; fresh available metadata does not resolve that request's acceptance.

Ownership: dashboard.tsx and the existing direct invocation browser fixtures/tests only. No server/http/journal/auth changes; backend owns private inquiry intervention concern3946890562.

- [x] Central normal invoke guard and visible controls reject new/changed submissions while recovery is unresolved, even if refreshed availability is available. Preserve original capability/body/role/key; unchanged fields do not bypass the lock through ordinary invoke.
- [x] Only lookupOnly recovery may proceed during uncertainty. Successful lookup binds the original run. Absent/failed lookup remains unconfirmed and cannot auto-unlock new requests or fall back to invoke.
- [x] Provide an explicit separate-request action if the user chooses to abandon this local recovery. Explain that the original request may still run or may have completed and this action does not cancel it. The action clears only local attempt state, makes no API call, and does not auto-submit. Actual subsequent submission still needs current available metadata and a newly generated key. Never clear journal/unknown safety state. Disable abandoning during an active request/probe to prevent late response retargeting.
- [x] Reproduce actual accepted-then-completed/lost-response/availability-available path; mutate inputs/role/capability and force form submit, assert no new invoke/key before resolution. Lookup sends captured original values+key+lookupOnly. Missing lookup remains locked. Explicit separate-request action alone creates no operation; a later deliberate submit gets a new key and still respects unavailable metadata.
- [x] Preserve privacy and existing response-loss/unknown tests. Focused RED/GREEN, build/typechecks/diff check, independent review, one serial full pre-push gate before updating existingPR97. No live target/demo or approval actions.

Task 3 implementation is complete locally; independent review, one serial full pre-push gate, and publication remain coordinator-owned.

### Task 4: Reconcile uncertain chat requests across chat and direct submission

PR97P1 comment3947199850: /api/chat can disconnect after service.invoke and even after the operation completes but before tool output arrives. onError currently only refreshes; a fresh user message has a new UUID and can repeat the operation. Direct-form Task3 does not cover chat.

Ownership: UI chat.tsx, transport.ts, session.tsx, bounded dashboard.tsx/main.tsx wiring if needed, existing focused browser/transport tests. No server/http/server-chat/journal/auth changes; B2 owns the agreed endpoint and active async integration. No new dependency or generic abstraction. Unit3 dialog changes are paused in another checkout; preserve Task3 direct guards here.

- [x] Trace all normal chat/direct submits, lookup recovery, stop, SDK errors and finish paths. Reuse one minimal per-session action admission/uncertainty guard in existing RunProvider so chat and direct requests cannot bypass each other. Retain immutable original chat body/idempotency key before network dispatch. Distinguish pre-dispatch ChatRequestError validation from a request whose acceptance is unknown. Guard function paths as well as visible controls; no new key or ordinary resend while uncertainty remains.
- [x] Use the installed AssistantChatTransport/AI SDK and native stream primitives to pin lifecycle callbacks to their exact outgoing request. SDK onFinish has isAbort/isDisconnect/isError/finishReason and message/messages, but its callbacks read latest options and messages can be current thread state; do not infer original attempt from a mutable latest-user reference when another request/thread has started. Handle aborted, truncated, failed, and clean streams; no automatic tool continuation/retry, no stream parser copied from the SDK. Prefer wrapping the existing transport's parsed UIMessageChunk stream for request-scoped completion if callbacks cannot be safely correlated. Keep AssistantChatTransport behavior. Local validation sends no POST and creates no uncertainty; a clean complete no-tool conversation may finish normally. Error/tool-error/partial/aborted responses cannot be treated as no operation.
- [x] While a shared uncertain action exists, newly typed chat may read status using the existing strict intent:status server contract only (no action tools), with a visible truthful explanation. Do not add an intent chooser or classify safety from user prose. Status replies, stale RunTool renders and unrelated run IDs cannot resolve the original key. New direct action submits remain blocked; direct lookupOnly recovery stays available for its retained original tuple. A direct uncertain request also prevents chat from creating an action. Keep the original hold through status requests/errors and late callbacks.
- [x] Agreed B2 contract: authenticated GET /api/chat/request with original Idempotency-Key header;200 exactly {kind:'run',runId,capability,state}. Caller authority is demoted for chat but subject retained; backend awaits exact journal.findRequest and authorized safe projection.400invalidkey,404missing/foreign fixederror,503storageunavailable. ZERO model/invoke/reserve/bindReference and no args/results/privatechilddetails. Use this exact lookup to reconcile a chat attempt; no replay of /api/chat or classifier fallback.404/503remain unconfirmed. A200 proves original run binding, not whole chat producer/linked-child completion; follow normal authoritative run/availability checks and never infer readiness from terminal parent alone. Coordinate any contract deviation with root/B2 first.
- [x] Only exact authoritative binding or explicit warned local abandonment resolves uncertainty. Reuse the existing separate-request concept: clear only local capture, warn original may still run or may have completed and is not canceled, zero API/autosubmit on abandonment, later deliberate submit freshkey/current admission. Prevent abandonment during active original transport or lookup; old callbacks must not clear/replace a new hold. Logout/session replacement aborts/clears local state. Preserve unknown quarantine, original request body/role, and no hidden journal reset.
- [x] Meaningful RED/GREEN: actual fixture accepts/completes original request, then loses stream before tool output; refresh can show available metadata. Attempt another explicit chat action and forced direct submit: zero fresh-key action dispatch until resolved/abandoned; status-only chat may use a distinct message ID/key but must expose no action tools and must not replace or clear the original hold. Lookup sends original key and invokes no model/runtime; missing/failed lookup stays locked. Confirm direct uncertainty prevents chat action, stop-before/after-output, original-key lookup success, parent terminal with linked work not ready, clean conversation and pre-send validation, delayed callbacks/thread or session changes, no-op abandonment followed by deliberate freshkey/current-readiness submission. Inspect actual network bodies and operation counts. Tests can use an offline read-only fixture for the agreed B2 endpoint while server implementation lands; clearly report that real endpoint integration is still pending.
- [x] Run smallest focused cases, existing affected chat/direct scenarios, types/build/diff; no fullCI/push or live requests. Commit scoped UI/tests only and exact scratch report at .superpowers/sdd/2026-09-07-ui-readiness/task-4-report.md (never commit report). Root owns scoped/final review/publication and combining the reviewed B2 endpoint before claiming complete integration. Backend server/http edits remain prohibited in this checkout.

Task 4 is complete and independently approved at `27c3b924dbf1312f2079c18fa5bd89e3313d72d1`. UI publication still requires the controller-owned full pre-push and exact-head hosted gates. Real authenticated endpoint and PostgreSQL browser acceptance remain B2 integration work and are not claimed here.
