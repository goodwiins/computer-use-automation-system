# Exact-run review entry and readable approval facts

Goal: implement approved UX unit3 using the existing approval endpoint and current RunProvider. Prepared worktree: /Users/goodwiinz/.codex/worktrees/interface-ai-ui-review, branch codex/meridian-ui-review. Dependency: reviewed PR97 head724519663c2d2fd650923de0234a7ff5fad834fe, local CI771/typechecks/Next build passed; hosted CI is running. API remains Express. Setup and smoke19 passed in this fresh checkout. Source spec is Task3 in docs/superpowers/plans/2026-09-07-meridian-ux-delivery.md.

### Task 1: One review dialog for the exact current run

Ownership: UI session.tsx, dashboard.tsx, main.tsx, review.tsx, minimal styles and existing browser tests. Reuse unit2 labels/decimal helpers; no server/auth/runtime/approval changes and no new dependencies.

- [ ] Add current reviewRunId/openReview/closeReview to RunProvider. Mount one native dialog in Workspace. Resolve the run from current authenticated state, not a cached copy of approval facts. Chat and history cards open this exact ID through one Review request button; merely opening or restoring it sends no invocation/decision.
- [ ] Move existing approval controls into that single dialog; no duplicate panels in chat/history. Reuse ApprovalPanel and its exact intervention ID, expiry, stale-connection lock, duplicate-click guard and uncertain-response probe. Caller sees only the server's waiting notice, never operator facts or decisions. Missing run/intervention shows an unavailable/no-longer-pending state.
- [ ] Display public action.facts as readable named fields without dropping unknown provided facts. The publicIntervention projector already removes credentials; do not reconstruct hidden native facts. Show actual action operator/branch/role and expiry. Keep method/destination/artifact/run/version/step/control under Details. Escape hostile values via normal React rendering.
- [ ] Use Confirm transfer/open share/contact update/hold labels as applicable, with a safe Confirm request fallback. Posting refusal is Refuse request. Manual repair remains Retry after repair / Stop request with its explicit bounded-retry explanation. Missing context and expired/stale/uncertain state disable relevant decisions; no success/no-post inference from a failed request.
- [ ] Decision locks must survive closing/reopening the dialog and switching between runs within the same authenticated session. A response-loss state cannot become clickable merely because ApprovalPanel remounted. Keep the smallest session-scoped attempt state keyed by exact run/intervention; no global or persistent credential/approval cache. Pin in-flight callbacks to the original run/intervention and do not unlock a newer intervention from an older response. Session replacement clears it. Add regression coverage for close/reopen while POST/probe is pending and switch-away/back after an uncertain response.
- [ ] Native showModal/close/Escape plus focus restoration to the opener. Initial focus belongs to a neutral heading or Close control, never the posting confirmation; opening a review cannot dispatch any decision. Switching sessions unmounts and clears selection. Preserve server-updated pending facts while open; an old approval ID is never silently reused against another intervention.
- [ ] Adapt existing approval browser scenarios to the dialog entry path, then add the smallest missing checks: exact run from chat, one dialog across chat/history, caller restriction, focus/Escape, logout, changed/expired intervention, duplicate confirmation, response loss. Keep existing API rejection tests and no-execution assertions. Offline fixtures only; no real approval clicks.
- [ ] Focused tests then scheduled full CI, independent review, scoped commit/report. Coordinator owns publication and cross-branch integration; no subagents or pushes by implementer.
