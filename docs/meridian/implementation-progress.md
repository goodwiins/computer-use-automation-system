# MERIDIAN requirement and evidence matrix

Historical source inventory: `dev` merge `b3c838bb113fb6de3a966040e2997671c44102b3`, September 5, 2026. The [runbook](runbook.md) records the newer integrated baseline and approval-safety follow-up; per-row evidence below retains its original scope. This inventory covers [the completion plan](../plans/2026-09-04-meridian-superpowers.md), its linked [replacement design](../plans/2026-09-03-meridian-adaptation.md), and the [target-application supplement](../plans/2026-09-04-meridian-target-application.md). Status is requirement-level: **verified** means the cited source proves only the stated scope; **in progress** means some required evidence exists; **blocked** names a prerequisite that prevents safe execution; **pending** means no accepted evidence closes the requirement.

Live capability acceptance is **4/7**, including the verified open-share pair. Historical green checks do not establish current whole-plan acceptance. Mutation-intent count is not proof that a native POST reached the target.

## Baseline, shared runtime, and delivery rules

| ID | Plan reference and requirement | Implementation or missing boundary | Source-specific verification evidence | Status |
| --- | --- | --- | --- | --- |
| BASE-01 | Current integrated baseline; Tasks 1–3 and supplement Task A | `cli.ts`; `src/runtime/{run,journal,contracts}.ts`; `src/surface/{browser,guarded}.ts`; `src/replay/executor.ts` | This historical checkpoint is `b3c838b`, merging reviewed PR #63 head `32e29f8` with 545 tests/17 files and typecheck; head CI `33988213912` / producer `101365583725` passed and exact-head code review completed without new findings. Security review was on ancestor `feef94b`. PRs #50–#53 remain integrated with their recorded gates. PR #54 merged evaluator event-type validation from reviewed head `5d71070`; local pre-push passed 395 tests/17 files and typecheck, head workflow `33958769455` / producer `101286792115`, merge workflow `33960952350` / producer `101292625264`, and exact-head code/security reviews passed without findings. Head and merge share tree `8ff8d528`. | verified |
| ARCH-01 | Global Constraints; replacement §§2–3: one process, one active run, visible fresh browser, filesystem journal, no concurrent pool | `src/runtime/run.ts:createRuntime`; `src/runtime/journal.ts:Journal`; `src/server/service.ts:InvocationService` | Source has the shared runtime and exclusive journal; existing lifecycle/API tests exercise contention and restart behavior. This is the documented demo concurrency ceiling. | verified |
| AUTH-01 | Global Constraints; replacement §3: separate caller/operator principals, caller allowlist, supervisor and decision authority | `src/server/http.ts:createApp`; `src/server/service.ts:InvocationService`; `src/runtime/run.ts:operatorContext` | `test/meridian.test.ts` covers 401/403, caller-bound chat, operator-run isolation, supervisor selection and decision rejection. The live read service separately passed authenticated catalog/history/status/evidence and caller approval denial. | verified |
| APPROVAL-01 | Global Constraints; Tasks 2–3, 9: single-use approval, stale/duplicate 409, unauthorized 403, abort/close/expiry safe, recheck immediately before dispatch | `src/runtime/approval.ts:Approval`; `src/surface/guarded.ts:GuardedSurface`; `src/escalation/operator.ts:OperatorConsole` | Current tests cover the deterministic approval/lifecycle matrix. PR #46 repaired the real terminal handoff wording/focus boundary; head workflow `33949006345` and merge workflow `33949275807` passed. No write-capability live acceptance is inferred. PR #65 hosted review subsequently reopened approval projection and terminal decision pairing. This branch repairs those paths; independent review and hosted delivery are still pending. | in progress |
| RISK-01 | Replacement §2; Tasks 3, 5–8: live effective risk controls discovery, replay, recovery and resume; down-label cannot bypass mutation policy | `src/runtime/profile.ts:classify`; shared discovery/replay profile handling; `src/surface/guarded.ts:GuardedSurface.click`; discovery fault routing | PR #50 closed the shared offline discovery detector gap and PR #51 wired the existing fault scenarios through discovery. Their reviewed heads `90ce880` and `0baf8df` and merge workflows passed with hosted code/security reviews reporting no findings. This closes shared offline parity and fault wiring only; operation-specific live discovery/replay, recovery and resume gates remain open. | in progress |
| JOURNAL-01 | Global Constraints; replacement §3: reserve before browser, signed request identity, same-key dedupe, durable intent, terminal unknown, no automatic resume | `src/runtime/journal.ts:{Journal,readJournalRecord,validateIdempotencyKey}`; CLI and service `beforeDispatch` hooks | `test/meridian-cli.test.ts` and `test/meridian.test.ts` cover reservation conflicts, restart, pre/post-intent failures and terminal state. The preserved open-share record remains unknown; see UNKNOWN-01. | verified |
| OUTCOME-01 | Supplement Task A: validated insufficient funds before intent is `business_outcome / INSUFFICIENT_FUNDS`; post-intent ambiguity stays unknown | `src/replay/outcomes.ts:InsufficientFundsError`; mappings in `src/{replay/executor,agent/loop}.ts` and `cli.ts` | Task A source `6ab82fe` passed 131 focused and 274 full offline tests; merged through PR #43 at `4f84b9f`. No live underfunded acceptance exists. | verified |
| CONTRACT-01 | Global Constraints; replacement §2: decimal strings/cents, schema-v1 compatibility, schema-v2 server references/select-by-value/typed tables, extract-backed outputs; schema and capability versions are distinct | `src/artifact/schema.ts:CapabilityArtifact`; `src/runtime/contracts.ts:{meridianContracts,applyMeridianContract}`; artifact executor paths | `test/schema.test.ts` parses and round-trips a schema-v1 fixture. The three approved MERIDIAN files are capability-version `1.0.0`, schema-v2 artifacts; `test/meridian-artifacts.test.ts` asserts schema 2 and their server references. A focused current-baseline run of both files passed 11 tests. Operation-specific write outputs remain gated below. | verified |
| CONTRACT-02 | Tasks 5–8 and shared contracts: validate discovery inputs before execution; every required public input remains executable in the promoted artifact | [PR #49](https://github.com/goodwiins/computer-use-automation-system/pull/49), reviewed source `e507e8a`, merged as `245ba838` | Canonical preflight, exact public declarations, executable binding checks and profile-scoped transfer setup refusal passed 197 focused/288 full tests, independent final re-review, head CI `33952285341` / `101269200295`, hosted code/security review and merge CI `33952963465` / `101271025160`. Review also fixed legacy ID collisions and ignored table-pattern bindings. No live operation or accepted artifact changed. | verified |
| TOOL-01 | Replacement §§3 and 5: export both OpenAI and MCP tool descriptors; an MCP server is not required | `src/artifact/tools.ts:toToolSchema`; catalog projection in `src/server/service.ts` | Source returns both descriptors from one public-input schema and excludes server-bound parameters. A direct pure current-baseline check on the approved member-record artifact verified OpenAI/MCP names, MCP capability version and server-input omission; `test/meridian.test.ts` also asserts OpenAI server-input omission. This is descriptor construction evidence, not an MCP server test. | verified |
| EVIDENCE-01 | Replacement §4; Task 9: authenticated evidence, dynamic-value masking, metadata-only unsafe capture, observer isolation, stable terminal metadata | Shared logger/safe-event and masking paths; isolated repair in PR #47 | Baseline audit reproduced protocol/status corruption from sensitive-value collisions. Repair `66a9fb0` passes 278 tests/17 files, validation, scoped and independent final review, and hosted CI `33950233434` / producer `101263489851`; merged at `2c7f4ed` with merge CI `33950705840` / producer `101264800819` passed. Historical evidence is preserved. This closes the scoped metadata defect, not general evaluator soundness or live-write acceptance. PR #65 hosted review subsequently reopened approval projection and terminal decision pairing. This branch repairs those paths; independent review and hosted delivery are still pending. | in progress |
| PR-01 | Global Constraints; Required PR boundaries: each remaining major task gets its own isolated `codex/` PR to `dev`; final merge and production are separate | Separate runtime PRs #61–#63 merged; maintenance recovery is prepared on `codex/meridian-maintenance-recovery` | Runtime/docs PRs #39, #43–#54 followed the boundary. Open-share, update and hold runtime PRs #61–#63 are merged to `dev`. Maintenance recovery is a separate PR-sized change. Accepted transfer/write artifacts and final acceptance delivery remain outstanding. | in progress |

## Seven capability requirements

| ID | Plan reference and requirement | Implementation or missing boundary | Source-specific verification evidence | Status |
| --- | --- | --- | --- | --- |
| CAP-01 | Task 4; replacement §2: sign-on returns authenticated operator, branch and role | `artifacts/meridian-sign-on.v1.0.0.json`; shared discovery/replay runtime | Approved artifact plus genuine discovery/replay are listed in `docs/meridian/live-evidence.md`; fresh PR #44 source read passed with 8 attempts, evaluator pass and zero intents. | verified |
| CAP-02 | Task 4; replacement §2: member inquiry supports number/last-name lookup and does not auto-select ambiguous matches | `artifacts/meridian-member-inquiry.v1.0.0.json`; typed member table contract | Approved artifact plus genuine discovery/replay exist; fresh PR #44 source read passed with 10 attempts and zero intents. Earlier authenticated API evidence covers number/name lookup and ambiguity without selection. | verified |
| CAP-03 | Task 4; replacement §2: member record returns share IDs, types, decimal balances and statuses | `artifacts/meridian-member-record.v1.0.0.json`; typed shares extraction | Approved artifact plus genuine discovery/replay exist; fresh PR #44 source read passed with 10 attempts and zero intents. | verified |
| TR-01 | Tasks 5a–5b: bind selected member, distinct eligible shares, funds, all review facts and canonical output before success | `src/runtime/contracts.ts:{assertTransferEligibility,assertTransferFacts,assertTransferOutputs}`; `src/surface/guarded.ts`; discovery/replay completion hooks | PR #39 runtime regressions passed; PRs #44–#45 repaired the actual CLI inspection loader and sibling review/form association. PR #45 final head and merge CI passed. These are runtime checks, not artifact acceptance. | verified |
| TR-02 | Task 5c feasibility: establish observed result structure, confirmation relationship and header handling before promising a candidate | Hosted receipt mapping and confirmation verification remain to be established before a candidate claim | Frozen/read-only evidence does not establish the canonical transaction result structure: the historical completed discovery froze a masked receipt with omitted values, which is insufficient for reconstruction. Generic grouped extraction feasibility is now verified by [PR #59](https://github.com/goodwiins/computer-use-automation-system/pull/59) (merged to `dev` at `7d0111f9798bbed2d5b510c6350c3cb329dbdb8d`, reviewed head `63c4b44`, 406 tests/full typecheck and head/merge CI passed), with validated text-free table topology in strict sidecars and the native-CSS-columns versus Playwright-target distinction preserved. Current hosted receipt mapping remains pending; live acceptance remains 3/7. | in progress |
| TR-03 | Task 5c: genuine complete post-hardening recording, provenance review, no Important/Critical findings, promote, targeted artifact gate | Missing tracked `artifacts/meridian-funds-transfer.v1.0.0.json` on current tree | Original successful discovery and draft remain preserved but cannot be promoted. Four later pre-approval attempts plus later handoff attempts ended terminal failure with zero intents. [Selected-input run](evidence/transfer-approval-expiry-sep6/summary.json) at `a80ac4e` reached native review and the personal approval gate, then expired after five minutes (17 actions, zero intents). No replacement candidate exists. | blocked |
| TR-04 | Task 5d: separately approved API/operator replay, output and resulting-state verification, same-key dedupe, sanitized evidence, capability PR | Missing approved artifact and replay | No replay exists. Depends on TR-02 and TR-03; the old discovery balance change cannot serve as replay evidence. | blocked |
| CAP-04 | Task 5 completion: accepted funds transfer raises live coverage to 4/7 | Missing accepted artifact/replay pair | One historical approved discovery post has evaluator pass and independently verified resulting state; artifact is still draft and all replay gates are open. | in progress |
| CAP-05 | Task 6: exact member/type/deposit and genuinely new share; discovery and separate replay | Recorded receipt extractor plus existing bound review and fresh member-state completion validator | [Reviewed discovery/replay pair](evidence/open-share-pair-sep6/discovery.json) and [replay](evidence/open-share-pair-sep6/replay.json) both passed signed evaluation after distinct personal approvals. One stale private inquiry checkpoint was removed in review; no steps were invented. [Restored caller catalog/history and same-key dedupe](evidence/open-share-pair-sep6/service.json) passed with unchanged journal and no runtime. Historical unknown opening remains frozen. | verified |
| CAP-06 | Task 7: member update binds exact member/contact fields and verifies saved values by fresh read; abort first; discovery and separate replay | Implemented exact native member/contact checks, GET-entry route binding, frame/origin approval and dispatch checks, and fresh observed contact readback in `src/runtime/contracts.ts`, `src/runtime/run.ts` and `src/surface/guarded.ts`; discovery, replay and API callers await completion validation | PR #62 merged at `4886ad6` from reviewed head `af95ba2`; 481 tests/17 files, typecheck and head/merge CI passed. Offline checks cover the GET-entry path and same-path cross-origin redirect rejection while preserving valid second-origin readback. Genuine artifact, approved discovery and separate replay, abort, and persistence evidence remain missing. This capability remains incomplete; total live acceptance is 4/7. | in progress |
| CAP-07 | Task 8: natural teller denial and separately approved supervisor hold, exact facts, fresh HOLD read, discovery and replay | Offline hold runtime covers selected member and unique `OPEN` share eligibility, supervisor/native and visible fact checks, a complete unique canonical review table, stage/frame/origin approval and dispatch guards, plus a post-approval read-only same-session UI-tab reread of share status/type/name followed by a final original review/token/frame recheck. Central wrong-member navigation is checked before the GET and the readback uses a shared deadline in `src/runtime/contracts.ts`, `src/surface/guarded.ts`, `src/runtime/run.ts` and `src/replay/executor.ts`; callers await completion verification. The auxiliary eligibility read permits exactly one bound top-level member document request, disables its page scripts, drains auxiliary pages and retains its strict request guard through cleanup. The UI reread is non-atomic, so concurrent post conditions remain a target-side requirement. | PR #63 merged to `dev` at `b3c838b`. Offline source `32e29f8` passed 545 tests/17 files, typecheck and scoped final reviews, including initial navigation-only and GET-form coverage. The auxiliary read uses server-rendered member data while the original review stays active. Genuine live approval, artifact, discovery/replay and evidence remain pending. The existing teller warning is only a read observation, not a denial. This capability remains incomplete; total live acceptance is 4/7. | in progress |
| UNKNOWN-01 | Global Constraints; Tasks 6 and 9: preserve unknown open-share operation without retry, deletion or reclassification | Authenticated journal and `docs/meridian/evidence/unknown-posting/` | Dashboard restart/read evidence recorded the signed envelope unchanged and no new run. Current source maps post-intent uncertainty to terminal `POST_OUTCOME_UNKNOWN`. | verified |

## Exception and deterministic safety acceptance

Repeated scope for EXC-02 through EXC-07: the existing profile probes are **read-only, pre-intent, per-request operation-route observations**. They prove their recorded detector/profile response only; they do not prove operation-specific discovery/replay classification, same-browser completion, or any write. PRs #50–#51 now provide shared offline discovery detector and fault-routing coverage. EXC-03 also has a native absent-member discovery/replay pair. Fresh injected discovery stops after PR #87 now verify the discovery halves of EXC-02/03/05/07; their replay halves remain open. See [current live evidence](live-evidence.md#injected-discovery-checks-after-the-checkpoint-repair).

| ID | Plan reference and requirement | Implementation or missing boundary | Source-specific verification evidence | Status |
| --- | --- | --- | --- | --- |
| SAFE-01 | Task 9 deterministic matrix: rejected/stale approvals, changed facts/role/destination/token/body, restart identity, crash phases, post-intent no-retry, caller isolation, masking, typed outputs, inert HTML/evidence auth | Existing shared approval, guard, journal, service, evidence and frontend paths; `test/meridian.test.ts` and related focused tests | Task 9a recorded 66 focused tests at `541d776`; later current-tree suites include the repairs cited in BASE-01. This proves deterministic/offline scope only. | verified |
| EXC-01 | Supplement Task B: natural bad login, controlled failure, sanitized evidence, no member action/mutation | Approved sign-on artifact through shared runtime, synthetic nonexistent operator, one attempt | [Sanitized evidence](evidence/natural-bad-login/summary.json): run `2fe15d0d-1b74-4905-8cc1-246ab207f439` at source `2c7f4ed` observed actual invalid-credentials rejection and NOT SIGNED ON; terminal/authenticated journal failure, evaluator pass/task failure, six attempts, zero intents, one sign-on POST response and zero member/mutation requests. No discovery or HTTP API claim. | verified |
| EXC-02 | Task 9 / supplement: validation response; preserve pre-/post-intent distinction | Profile detector/fault hook exists; accepted D/R operation evidence is incomplete | `docs/meridian/evidence/profile-probes/summary.json` records a zero-intent read-only validation probe. Native submitted-form behavior and required D/R ledger remain open. Fresh [injected discovery](evidence/injected-prepost-sep6/validation.json) at `eedc4ea` stopped with `VALIDATION_REJECTED`; signed evaluator PASS, 11 actions, zero intents. Replay remains missing. | in progress |
| EXC-03 | Task 9 / supplement: injected not-found and absent member both produce no alternate selection | Native absent-member CLI discovery/replay is verified; injected discovery is verified; injected replay is missing | [Discovery](evidence/native-notfound/discovery.json) `db95cb76-03a4-471c-b928-b53dccce9272` stopped with `NO_SUCH_MEMBER` and journal failure; [replay](evidence/native-notfound/replay.json) `5ba0ad25-4a38-4c8e-a6d7-9d6e0cdb6d3c` returned `business_outcome / NO_SUCH_MEMBER`. Same synthetic query, separate fresh keys/journals, source `0baf8df`; both evaluator pass, nine attempts, zero intents and zero observed non-sign-on POST requests. Native `GET /members` responses were observed; no alternate member or write. The authenticated, hash-unchanged pair also [passed](evidence/native-notfound/reevaluation.json) evaluator source `5d71070`, merged by PR #54 as current `601e266` with passing head/merge CI. The older injected probe is not discovery/replay evidence, so overall EXC-03 remains open. Fresh [injected discovery](evidence/injected-prepost-sep6/notfound.json) at `eedc4ea` stopped with `NO_SUCH_MEMBER`; signed evaluator PASS, 10 actions, zero intents. Injected replay remains missing. | in progress |
| EXC-04 | Task 9 / supplement: permission and natural teller hold denial; no supervisor upgrade | Role guard and permission detector exist | Zero-intent permission probe exists; teller page warning is not a demonstrated denial. Task 8 must supply the natural teller case. | in progress |
| EXC-05 | Task 9 / supplement: timeout and natural idle expiry stop before intent; no relogin/retry | Session-expiry detector and no-login-recovery policy exist | Zero-intent timeout probe exists. [Natural idle evidence](evidence/natural-idle/summary.json) records run `fd01920a-dac4-4550-8f57-e04e5c6ad80c` at `e507e8a`: 540008 ms idle, then read success, signed journal/evaluator pass, 10 attempts, zero intents, one sign-on POST response and no relogin. Expiry was not observed; full D/R expiry acceptance remains open. Fresh [injected discovery](evidence/injected-prepost-sep6/timeout.json) at `eedc4ea` stopped with `SESSION_EXPIRED`; signed evaluator PASS, 12 actions, zero intents and no action after detection. Injected replay and natural expiry remain open. | in progress |
| EXC-06 | Task 9 / supplement: maintenance allows one known pre-intent same-browser recovery and checkpoint revalidation; menu alone is not success | Offline trusted one-shot open-share recovery is implemented for the observed same-browser/member/frame path: member checkpoint validation and exact clean re-entry are guard-owned, with strict missing-hook or omitted-resume fail-closed handling; other operations remain unsupported | [Historical zero-intent observation](evidence/maintenance-observation/summary.json) `b7fefa1a-450d-4502-b827-747764cd8d0c` reached the member record and clean operation re-entry after Continue, correcting the earlier menu assumption. Its producing Git SHA was not captured; static checkpoint counts do not establish displayed member-value binding. This is target-structure evidence, not verification of the current recovery hook. Genuine discovery/replay recovery acceptance remains pending, and no live acceptance is added beyond 3/7. | in progress |
| EXC-07 | Task 9 / supplement: server 500 is failure pre-intent; any unverified post-intent response is unknown | Server detector and executor unknown-outcome path exist | Zero-intent server probe exists; accepted D/R and post-intent evidence are not complete. Fresh [injected discovery](evidence/injected-prepost-sep6/server.json) at `eedc4ea` stopped with `APPLICATION_ERROR`; signed evaluator PASS, 13 actions, zero intents. Replay remains missing. | in progress |
| EXC-08 | Task 9 / supplement: natural valid underfunding returns typed business outcome with zero intent/POST | `InsufficientFundsError` and shared discovery/replay/CLI mapping | Offline implementation is verified under OUTCOME-01. [Discovery preflight](evidence/natural-underfunded-sep6/summary.json) at `2d539b5` verified native member-share underfunding with `business_outcome / INSUFFICIENT_FUNDS`, agreeing signed journal/result and evaluator PASS (9 actions, zero intents). API/replay agreement and independent native network-counter evidence remain unverified. | in progress |
| EXC-09 | Task 7 / Task 9: natural invalid email/phone, with honest pre-/post-intent state | Member-update runtime merged in PR #62; accepted target observation is missing | Depends on CAP-06; no accepted run exists. | blocked |
| EXC-10A | Task 9: deterministic missing/changed-token refusal occurs before dispatch; any unverified failure after intent remains unknown with no repeat | Guard token/form checks and shared unknown-outcome mapping | `test/meridian.test.ts` changes or removes the inspected token before dispatch and asserts refusal with zero POST; CLI/replay lifecycle tests preserve post-intent `POST_OUTCOME_UNKNOWN` and no retry. | verified |
| EXC-10B | Supplement Task B ruling: capture a hosted stale-token server rejection only if encountered; do not create a posting to manufacture it | Existing native-form and unknown-outcome paths would classify an encountered case by actual intent phase | No hosted stale-token rejection has been encountered. This conditional observation is untriggered and is not a completion gate; deterministic coverage is recorded in EXC-10A. | verified |
| EXC-11 | Supplement Task B: unexpected dialog is controlled and cannot trigger an unsafe repeat; capture hosted evidence only if encountered | Replay dialog drain/controlled failure path | `test/fixes.test.ts` verifies an unexpected dialog is dismissed and reported as a controlled failure. No hosted dialog has been encountered; the conditional hosted capture is untriggered and is not a completion gate. | verified |

## Integrated demonstration, documentation, and final gates

| ID | Plan reference and requirement | Implementation or missing boundary | Source-specific verification evidence | Status |
| --- | --- | --- | --- | --- |
| UI-READ-01 | Task 9c–9d: authenticated dashboard read, reload, roles, evidence view, Refresh/Send keyboard behavior, elapsed time and historical redaction | `src/server/public/app.js`; existing HTTP/service paths | Genuine dashboard run and independent restart check passed at their recorded sources; elapsed renderer fixture covers known/zero/missing values. Scope is read-only. | verified |
| DEMO-01 | Task 9: accepted balance read plus separately approved transfer replay through API/operator controls; same-key repeat returns original run | Missing accepted transfer artifact/replay | Depends on TR-03 and TR-04. No integrated write rehearsal exists. | blocked |
| DEMO-02 | Task 9: exceptional outcome plus one safe same-browser repair, keeping repair separate from discovery provenance | Missing accepted same-browser completion evidence | A read-only observation reached the member checkpoint and clean open-share re-entry. Offline one-shot recovery passes regression checks; no accepted discovery/replay repair pair exists. | pending |
| DEMO-03 | Task 9: approval/handoff controls work by keyboard during a real write | Existing dashboard decision controls; no accepted keyboard write evidence | Read-only keyboard Refresh/Send passed. Approval/handoff controls were not exercised. | pending |
| DOC-01 | Task 9: accurate live ledger, 1–2 page report, exact setup/discovery/replay/demo commands, environment names, sanitized backup, labeled fallback modes, no readiness claim | [PR #48](https://github.com/goodwiins/computer-use-automation-system/pull/48), reviewed pre-fix checkpoint `2a28d1a` | Matrix and current checkpoint docs passed scoped, whole-branch and final re-review; accepted read/offline commands and current source status were restored. 278 tests/17 files, validation and head CI `33952265251` / producer `101269148717` passed. Hosted review then found the unpublished UI commits were not remotely resolvable; this update labels that boundary. Integration and final accepted-write delivery remain pending. | in progress |
| CMD-01 | Tasks 5c–8: per-capability promotion is review-only, then targeted artifact test; each terminal run uses authenticated evaluator | Existing `cu replay --approve`, `test/meridian-artifacts.test.ts`, `scripts/evaluate-run.ts` | Commands are named in Tasks 5c–8. They cannot run successfully for missing write artifacts, and promotion does not authorize a transaction. | blocked |
| CMD-02 | Required PR boundaries: each changed capability PR runs its focused regressions, `npm run ci`, `npm run validate`, `git diff --check`, then exact-head hosted checks | Existing package scripts and GitHub workflow | Completed runtime PRs have source-specific results. PRs #61–#63 passed their source-specific gates. Integrated maintenance source passed 561 tests/17 files, typecheck and artifact validation; hosted publication gates remain to be recorded. Final live acceptance gates remain outstanding. | in progress |
| FINAL-01 | Task 9 final delivery: all seven accepted, required unhappy paths, sanitized evidence, integrated rehearsal, final local gates and same-head hosted CI; verify merged `dev` separately | Remaining live capability acceptance and final-acceptance PR are missing | Current core baseline is PR #87 merge `1a97742`, with reviewed head `f33ca72`, 652 tests and passing head/merge CI. This is a runtime baseline, not the final acceptance head. No production deployment is included. | blocked |
| SERVICE-OUTCOME-01 | Assignment §3.4: discovery and replay preserve deliberate business-outcome reporting in history | Shared allowlisted discovery detector completion; existing CLI/journal/service paths | PR #88 merged `84fdd6c`; 707 tests and head/merge CI passed. [Native absent-member discovery and restored service](evidence/service-completion-sep6/discovery-outcome.json) at `691cdf0` agree on `business_outcome / NO_SUCH_MEMBER`, signed evaluator PASS, nine actions and zero intents. Post-intent unknown precedence remains covered offline. | verified |
| SERVICE-HISTORY-01 | Assignment §§3.4–3.5: historical input/output structure without persisted raw values | Runtime/logger record strict field/type metadata; service validates it; dashboard shows withheld values and unavailable legacy fields | PR #89 merged `27767cf`; 710 tests, both typechecks/build and head/merge CI passed. [Real read, restart and UI](evidence/service-completion-sep6/historical-structure.json) at `e9ab9d5` verified exact safe persisted metadata, unchanged signed journal and zero new UI invocations/decisions. Older records remain unavailable where structure was not captured. | verified |
| UI-FINAL-01 | User-selected Vercel AI SDK + assistant-ui chat; preserve auth/operator gates and authoritative run identity; final integration acceptance after capability/runtime/API gates | PR #84 merged as `a80ac4e`, tree-identical to reviewed head `53a6f56` | 699 tests/23 files, both typechecks/build and head/merge CI passed. [Missing-member rehearsal](evidence/ui-final-sep6/missing-member.json) reached Search and returned the correct business outcome; explicit status lookup preserved the run/journal, and reconnect created no duplicate. [Final-head balance rehearsal](evidence/ui-final-sep6/balance.json) also passed with 35 matching typed rows and unchanged journal after status lookup. Earlier failed attempts remain recorded. Final write demonstration remains open. | in progress |

## Next safe steps

1. Keep PR #54's event-type repair and authenticated native-pair re-evaluation scoped; passing head/merge gates do not establish universal evaluator soundness or Task 9 completion.
2. Preserve every failed transfer run and the frozen unknown open-share operation. The original share selection included a non-`OPEN` share. The user selected a replacement pair, and its recording reached review before the approval expired. Revalidate those selected facts for the next intentional recording; do not reuse the expired run or approval.
3. Establish transfer result extraction from genuine observed structure. If it cannot be supported, make only the reviewed minimal extractor/contract repair before any new candidate claim.
4. With a genuinely selected operation and a separate terminal approval, record a complete transfer candidate; review its provenance and outputs, promote it without posting, then request a separately approved replay and verify resulting state.
5. Execute Tasks 6, 7 and 8 in order, each with capability-specific observed validators, discovery/replay approvals, result reads, sanitized evidence and its own PR.
6. Complete the natural/injected exception ledger, integrated write and same-browser repair rehearsals, approval/handoff keyboard check, current documentation, and final local/head/merge gates.
7. Coordinate the independently authorized Vercel AI SDK/assistant-ui task, verify its exact commit and PR, and perform final integrated UI acceptance after the remaining capability and safety gates. Resolve any remaining visual/interaction choices with the user before extending scope.
